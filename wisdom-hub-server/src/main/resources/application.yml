# application.yml
server:
  port: 8080

spring:
  application:
    name: wisdom-hub
  profiles:
    active: dev


logging:
  level:
    com.wisdomhub: DEBUG
    org.springframework.mail: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n'