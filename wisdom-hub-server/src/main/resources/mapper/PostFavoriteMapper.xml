<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wisdomhub.mapper.PostFavoriteMapper">

    <resultMap id="PostFavoriteResultMap" type="com.wisdomhub.entity.PostFavorite">
        <id property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <!-- 收藏 -->
    <insert id="insert">
        INSERT INTO tb_post_favorite (post_id, user_id, create_time)
        VALUES (#{postId}, #{userId}, NOW())
    </insert>

    <!-- 取消收藏 -->
    <delete id="delete">
        DELETE FROM tb_post_favorite
        WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <!-- 查询是否已收藏 -->
    <select id="findByPostIdAndUserId" resultMap="PostFavoriteResultMap">
        SELECT * FROM tb_post_favorite
        WHERE post_id = #{postId} AND user_id = #{userId}
    </select>

    <!-- 统计收藏数 -->
    <select id="countByPostId" resultType="int">
        SELECT COUNT(*) FROM tb_post_favorite
        WHERE post_id = #{postId}
    </select>

    <!-- 查询用户收藏的帖子ID列表 -->
    <select id="findPostIdsByUserId" resultType="long">
        SELECT post_id FROM tb_post_favorite
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

</mapper>